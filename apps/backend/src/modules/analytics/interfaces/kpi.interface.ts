export interface KpiData {
  activeUsers: number;
  requestsToday: number;
  successRate: number;
  avgResponseTime: number;
  errorCount: number;
  transactionsPerMin: number;
  uptime: number;
  timestamp: string;
}
